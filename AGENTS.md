# Repository Instructions

- After making any code or file change in this repository, always create a git commit and push the latest commit to GitHub.
- Use the `main` branch as the default branch unless the user explicitly requests a different branch.
